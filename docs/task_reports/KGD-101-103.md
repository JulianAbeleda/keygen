# KGD-101–103 report

Implemented the frozen arm64 macOS identity and reusable-layer boundary check.

Changed paths:

- `crates/kg_ddlc_plus/src/identity.rs`
- `crates/kg_ddlc_plus/src/lib.rs`
- `scripts/check-module-boundaries.py`
- `docs/decisions/KGD-101-arm64-macos.md`
- `docs/decisions/KGD-102-identities.md`

Validation:

- identity unit test passes;
- `python3 scripts/check-module-boundaries.py` passes;
- workspace formatting, clippy, and tests remain required at integration.

The deployment floor is explicitly recorded as macOS 15.0 pending native-host
qualification. No Intel/universal or non-macOS product packaging is added.
