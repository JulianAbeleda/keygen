# KGD-002–006 report

Status: complete for the governance slice.

## Delivered

- `scripts/check-private-content.py`: tracked-file scanner with explicit,
  narrow recovery-path and absolute-path patterns, hash-context filtering, and
  synthetic self-test. It never traverses ignored local recovery data by
  default.
- `scripts/private/observation-protocol.md`: read-only observation checklist
  and redacted evidence template.
- `scripts/private/observe-dry-run.sh`: validates a selected redacted record
  without source discovery or installation writes.
- `docs/decisions/KGD-006-provenance-license.md`: local packaging boundary,
  provenance fields, prohibited repository content, and unresolved legal gate.

## Acceptance

```text
python3 scripts/check-private-content.py --self-test   # pass
python3 scripts/check-private-content.py                # pass
```

Only synthetic temporary fixtures are used by the scanner test. No recovered
assets, source, screenshots, absolute host paths, or private fixtures were
added to the repository. Cargo files were intentionally not modified.

## Limitations

The scanner is a guardrail, not a license determination or a binary-content
inspection tool. Distribution remains blocked until the provenance/licensing
review is separately resolved.
